<div class="container">
    <div class="box">
        <div class="profile">
            <article class="panel is-primary">
                <p class="panel-heading">
                    Mi Perfil
                </p>
                <%= link_to 'Mis direcciones', addresses_path, class:"panel-block is-active" %>
                <%= link_to 'Editar información', edit_user_registration_path, class:"panel-block is-active" %>
            </article>

            <div class="tile is-parent">
                <article id="profile" class="tile is-child notification is-primary ">
                    <div class="content">
                        <div class="tittle">
                            <%= current_user.first_name %>, <%= current_user.last_name %>
                        </div></br>
                        <div class="subtitle">
                            <%= current_user.phone %>
                        </div>
                        <div class="content">
                            <%= current_user.email %>
                        </div>
                    </div>
                </article>
            </div>


        </div>

    </div>
</div> 