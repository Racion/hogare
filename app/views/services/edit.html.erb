<% if current_user.admin.present? %>
    <div class="container">
    <div class="box">
        <div class="form">
            <h1 class="tittle">Asignar empleados</h1></br>
            <%= render 'form'%></div>
        </div>
    </div>
    </div>
<% end %>